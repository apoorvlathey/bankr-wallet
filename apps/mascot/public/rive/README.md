# Rive Runtime Exports

Place the editor-exported prototype here as:

```text
walletchan-mascot.riv
```

Do not place editable source files here. Store a `.rev` backup, when available,
under `apps/mascot/art/working/`.

The `.riv` must embed or locally reference every asset. Do not use Rive CDN
hosting for the wallet experiment.
