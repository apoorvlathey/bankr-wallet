/** Stable post-confirm asset-change extraction compatibility facade. */

export {
  extractAndStoreAssetChanges,
  extractAndStoreDestinationAssetChanges,
  type DestinationExtractionArgs,
  type SourceExtractionArgs,
} from "./history/assetChangePersistence";
