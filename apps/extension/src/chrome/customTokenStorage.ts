/** Stable compatibility facade for custom-token storage. */
export {
  addCustomToken,
  getCustomTokens,
  removeCustomToken,
  updateCustomToken,
} from "./tokens/customTokenStorage";
export type { CustomToken } from "./tokens/types";
