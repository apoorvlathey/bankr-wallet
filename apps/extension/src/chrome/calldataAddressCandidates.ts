/** Stable compatibility facade for bounded calldata address extraction. */
export {
  extractCalldataAddressCandidates,
  MAX_CALLDATA_ADDRESS_CANDIDATES,
} from "./tokens/calldataAddressCandidates";
