# Watch-asset confirmation UI audit map

- `WatchAssetConfirmationScreen.tsx` renders EIP-747 token review and the user
  decision.
- The root compatibility component exposes the existing import boundary.

Request provenance, expiry, and token-storage commits stay in the background
watch-asset domain.
