# Portfolio tests

Pure portfolio state, cache, chart, and token-key tests live here. The browser
navigation test remains explicit runtime QA and is excluded from the recursive
security runner by basename.
