# Frozen compatibility fixtures

Released storage shapes live here as immutable test inputs. Upgrade tests must
read these fixtures without normalizing or rewriting them, so compatibility
coverage remains independent from current record constructors.
