# Twitter Thread: How WalletChan Simulates Transactions With Zero External APIs

---

## Tweet 1 (Hook)

Every wallet shows you "asset changes" before you sign a transaction.

Most of them call a centralized API to do it.

We don't. Here's how WalletChan simulates every transaction using nothing but raw RPC calls and a ghost contract that never exists onchain:

> **[IMAGE: Hero graphic — split screen. Left side: other wallets with arrows going to cloud servers labeled "Alchemy / Tenderly / Blowfish". Right side: WalletChan with a single arrow going directly to an RPC node. Title: "Fully Decentralized Transaction Simulation"]**

---

## Tweet 2 (The Problem)

When a dapp asks you to swap 1 ETH for USDC, you want to know EXACTLY what leaves and what arrives.

The standard approach? Send the calldata to a third-party API, trust their servers to simulate it, and hope they return accurate results.

That's a single point of failure. And a privacy leak.

---

## Tweet 3 (The Core Idea)

Our approach: make the EVM do the work itself.

We wrote a Solidity contract called `TxSimulator.sol` — but we never deploy it.

Instead, we inject its bytecode into the user's address using `eth_call` state overrides and run the entire simulation in one atomic RPC call.

Zero servers. Zero trust assumptions. Just the EVM.

> **[IMAGE: Diagram showing the eth_call with state overrides. Show a box for "User's Address 0xABC..." with an arrow saying "inject TxSimulator bytecode here". Inside the box, show: (1) snapshot balances, (2) execute tx, (3) snapshot again, (4) return diff. Label: "All happens inside a single eth_call — nothing hits the blockchain"]**

---

## Tweet 4 (Why Inject at the User's Address?)

This is where it gets fun.

By placing the simulator AT the user's address:

- `address(this)` == user's address, so `balanceOf(address(this))` returns the user's REAL token balances

- When it calls `to.call{value}(data)`, the target contract sees `msg.sender` == user's address

The simulation is indistinguishable from the real transaction. No impersonation tricks needed.

---

## Tweet 5 (Token Discovery — The Access List Trick)

But how do we know WHICH tokens to check?

Most wallets maintain giant token lists or query indexer APIs.

We use a single RPC call: `eth_createAccessList`

It dry-runs the transaction and returns every contract address + storage slot that was touched. For a Uniswap swap, that includes the router, pool, WETH, USDC — everything.

These become our "candidate" tokens.

> **[IMAGE: Visual showing a Uniswap swap transaction flowing through contracts. eth_createAccessList returns a list: [Router, Pool, WETH, USDC, ...]. Arrows point from the list to a "Try balanceOf() on each" step. Router/Pool return 0 or revert (crossed out). WETH/USDC return actual deltas (highlighted green/red).]**

---

## Tweet 6 (The Simulator Contract)

The `TxSimulator.sol` contract is ~60 lines of Solidity. Here's the core logic:

```solidity
function simulate(
    address to, uint256 value, 
    bytes calldata data, 
    address[] calldata candidates
) external returns (
    bool success, int256 ethDelta,
    address[] memory tokens, int256[] memory deltas
) {
    // 1. Snapshot ETH + token balances
    uint256 ethBefore = address(this).balance;
    for (uint i; i < candidates.length; ++i)
        before[i] = _tryBalanceOf(candidates[i]);

    // 2. Execute the actual transaction
    (success, ) = to.call{value: value}(data);

    // 3. Compute deltas, return only non-zero
    ethDelta = int256(address(this).balance) - int256(ethBefore);
    // ... filter and return token deltas
}
```

Non-token addresses (routers, pools) just revert on `balanceOf` — `_tryBalanceOf` catches that and returns 0. They get filtered out automatically.

---

## Tweet 7 (The Full Flow)

Putting it all together — when you click "Confirm" on a transaction in WalletChan:

**Step 1:** `eth_createAccessList` — discover all touched contracts

**Step 2:** `eth_call` with state override — inject TxSimulator at user's address, pass candidates, get balance deltas

**Step 3:** Enrich metadata — token list lookup + onchain multicall for name/symbol/decimals

**Step 4:** Fetch USD prices — CoinGecko with portfolio fallback

Two RPC calls for the simulation itself. Everything else is metadata.

> **[IMAGE: Horizontal pipeline/flowchart: "Transaction" -> "eth_createAccessList" -> "Candidates [0xA, 0xB, 0xC...]" -> "eth_call + TxSimulator" -> "Deltas [-500 USDC, +0.25 ETH]" -> "Enrich Metadata" -> UI showing the final asset changes card with red/green amounts]**

---

## Tweet 8 (The Permit2 Problem)

There's a subtle problem: Permit2 checks `extcodesize(msg.sender)`.

Since we injected code at the user's address, Permit2 sees code and calls `isValidSignature` (ERC-1271) instead of using `ecrecover`.

Our fix: TxSimulator implements `isValidSignature` that does the ECDSA recovery internally:

```solidity
function isValidSignature(
    bytes32 hash, bytes calldata signature
) external pure returns (bytes4) {
    // extract r, s, v from signature
    // ecrecover and verify
    return 0x1626ba7e; // ERC-1271 magic value
}
```

Permit2 calls our function, we do the same ECDSA check it would have done, and the simulation proceeds correctly.

---

## Tweet 9 (Retry with Storage Slot Discovery)

What if the simulation reverts because the user doesn't have enough tokens? (common for impersonated/view-only accounts)

We retry with **storage slot overrides** — and here's the trick to find the right slots:

Call `eth_createAccessList` on `balanceOf(user)` for each token. The access list tells us EXACTLY which storage slot holds the balance mapping.

Then we override that slot with a large balance and re-simulate. Works for any ERC-20 implementation — OpenZeppelin, USDC proxy, custom, whatever.

> **[IMAGE: Diagram showing: "eth_createAccessList({ to: USDC, data: balanceOf(user) })" returns "storageKeys: [0x1a2b...]". Arrow pointing to "Override that slot with 10^30". Then "Retry simulation" with green checkmark. Label: "Universal storage slot discovery — no hardcoded mappings"]**

---

## Tweet 10 (Batch Transactions)

ERC-5792 batch transactions add another twist: the wallet sends multiple calls as a batch (approve + swap, for example).

Normal `simulate()` can't handle this because state changes need to persist between calls.

`TxSimulator.sol` has a `simulateBatch()` function that executes all calls sequentially in a single EVM execution context. The approval from call 1 is visible to the swap in call 2.

One `eth_call`. Multiple transactions. Cumulative deltas.

---

## Tweet 11 (Comparison)

How does this compare to other wallets?

| | WalletChan | Typical Wallet |
|---|---|---|
| Simulation | Pure RPC | Alchemy/Tenderly API |
| Token discovery | eth_createAccessList | Pre-built token lists |
| Contract deployed? | Never | Sometimes |
| Privacy | No data leaves RPC | Tx data sent to 3rd party |
| Works on any chain | Yes (if EIP-2930 supported) | Depends on API coverage |
| Contract size | ~1.3 KB | N/A |

---

## Tweet 12 (CTA / Closing)

The entire simulator is ~60 lines of Solidity and ~500 lines of TypeScript.

No API keys. No vendor lock-in. No trust assumptions beyond your RPC node.

Just the EVM doing what it does best — executing code.

If you're building a wallet and want to see the full implementation:

[link to WalletChan]

> **[IMAGE: Screenshot of the actual WalletChan asset changes UI during a swap — showing the red "SEND" section with token amount and USD value, and blue "RECEIVE" section. Clean, real product screenshot.]**

---

## Notes for Image Generation

1. **Tweet 1 image**: Split comparison (centralized vs decentralized). Use clean, minimal style. WalletChan branding colors.

2. **Tweet 3 image**: Technical diagram but visually clean. Show the eth_call "bubble" containing the entire simulation flow. Emphasize that nothing touches the blockchain.

3. **Tweet 5 image**: Access list visualization. Show a real-ish Uniswap swap path and how candidates are discovered vs filtered.

4. **Tweet 7 image**: Horizontal pipeline from raw transaction to final UI. This is the "money shot" — the end-to-end flow.

5. **Tweet 9 image**: Storage slot discovery flow. Show the clever reuse of eth_createAccessList for a different purpose.

6. **Tweet 12 image**: Real product screenshot. Authenticity > polish here.

Optional: A short screen recording / GIF of the simulation loading in real-time on an actual swap could replace the Tweet 12 static image and perform better on the algo.

---

## Image Generation Prompts

### Base Style (apply to all images)

Create a clean, readable technical infographic with subtle Bauhaus-inspired styling. It should be professional and easy to read at a glance, not an art piece.

STYLE RULES:
- 80% clean infographic, 20% Bauhaus flair — readability comes first
- White (#FFFFFF) background with lots of whitespace
- All text must be clearly legible — no text over busy backgrounds
- Thin black borders on key elements, hard offset shadows only on 1-2 focal elements
- No gradients, no soft shadows, no glow effects
- Sharp corners (no rounded corners)
- Sans-serif font throughout (like Outfit, Inter, or Futura)
- Color palette: Black #121212, Red #D02020, Blue #1040C0, Yellow #F0C020
- No competitor names or real logos
- No pixel art, no anime characters, no realistic illustrations
- Aspect ratio: 16:9 landscape (for Twitter/X post)
- The overall tone: clean enough to be corporate, stylish enough to stand out
- SUBTLE BAUHAUS ACCENTS: One small yellow (#F0C020) circle and one small blue (#1040C0) triangle placed in the margins as decorative accents — NOT competing with the content

### Tweet 1 Image — "Fully Decentralized Transaction Simulation"

(See base style above, then add:)

TITLE: "Fully Decentralized Transaction Simulation" — bold, black (#121212), sans-serif font, centered at the top.

LAYOUT: Two columns separated by a thin vertical gray line. Plenty of whitespace.

LEFT COLUMN — "Other Wallets":
- At the bottom, 3 simple flat wallet icons in a row (small, gray/muted, with thin black borders)
- From each wallet, a thin arrow pointing up to a single cloud shape centered above them
- The cloud has a thick black border (3px) and a light red (#D02020 at 15% opacity) fill
- Inside the cloud: text "Centralized Simulation APIs" in bold black
- Below the cloud, small text "Your tx data sent to third parties" in red (#D02020)
- A small red X icon next to that text
- At the very bottom: label "Other Wallets" in uppercase, bold, black

RIGHT COLUMN — "WalletChan":
- One wallet icon on the left side (with a thick black border and a small hard offset shadow: 3px right, 3px down, solid black)
- One bold arrow pointing right to a server/node icon
- The server icon has a thick black border and a light blue (#1040C0 at 15% opacity) fill
- Above the server: text "RPC Node" in bold black
- Below the arrow flow, small text "Direct connection, no middlemen" in blue (#1040C0)
- A small blue checkmark icon next to that text
- At the very bottom: label "WalletChan" in uppercase, bold, black

### Tweet 3 Image — "eth_call State Override Injection"

(See base style above, then add:)

TITLE: "How the Simulation Works" — bold, black (#121212), sans-serif font, centered at the top.

LAYOUT: White background. A single large rounded-corner-free rectangle in the center representing the eth_call execution context. Plenty of whitespace around it.

CENTRAL DIAGRAM:
- A large white rectangle with a thick black border (3px) and a hard offset shadow (4px right, 4px down, solid black). This is the "eth_call bubble" — the entire simulation happens inside it.
- At the top of the rectangle, a label bar with a light blue (#1040C0 at 15% opacity) fill: text "eth_call with state overrides" in bold black
- Inside the rectangle, show a box labeled "User's Address 0xABC..." with a thick black border. A small arrow pointing into it labeled "inject TxSimulator bytecode" in blue (#1040C0)
- Below that box, inside the rectangle, show 4 numbered steps stacked vertically with small spacing:
  - Step 1: "Snapshot ETH + token balances" — with a small yellow (#F0C020) square bullet
  - Step 2: "Execute the transaction" — with a small red (#D02020) square bullet
  - Step 3: "Snapshot balances again" — with a small yellow (#F0C020) square bullet
  - Step 4: "Return the diff" — with a small blue (#1040C0) square bullet

BOTTOM LABEL:
- Centered below the rectangle: "All happens inside a single eth_call — nothing hits the blockchain" in bold black with a blue (#1040C0) underline accent bar

### Tweet 5 Image — "Token Discovery via Access List"

(See base style above, then add:)

TITLE: "Token Discovery — The Access List Trick" — bold, black (#121212), sans-serif font, centered at the top.

LAYOUT: White background. A horizontal left-to-right pipeline/flow. Three stages connected by bold black arrows. Plenty of whitespace.

STAGE 1 — LEFT:
- A rectangle with a thick black border and light yellow (#F0C020 at 15% opacity) fill
- Inside: text "Swap Transaction" in bold black at the top, and below it smaller text "eth_createAccessList" in blue (#1040C0)
- This represents the dry-run call

STAGE 2 — MIDDLE:
- A vertical list/table with a thick black border, representing the returned access list
- Title bar at top: "Touched Contracts" in bold black on a light gray (#F0F0F0) background
- 4 rows listed vertically, each with a small colored square bullet and a label:
  - "Router" — gray square bullet
  - "Pool" — gray square bullet
  - "WETH" — blue (#1040C0) square bullet
  - "USDC" — blue (#1040C0) square bullet

STAGE 3 — RIGHT:
- A rectangle with a thick black border labeled "Try balanceOf() on each" at the top in bold black
- Inside, show 4 rows matching the contracts from Stage 2:
  - "Router" — struck through text, with a red (#D02020) X icon and small text "reverts" in red
  - "Pool" — struck through text, with a red (#D02020) X icon and small text "reverts" in red
  - "WETH" — bold text, with a blue (#1040C0) checkmark and small text "+0.25 ETH" in blue
  - "USDC" — bold text, with a red (#D02020) arrow-down icon and small text "−500 USDC" in red
- The WETH and USDC rows should feel visually highlighted compared to the struck-through rows

BOTTOM LABEL:
- Centered below the pipeline: "Non-token contracts revert on balanceOf — filtered out automatically" in bold black with a yellow (#F0C020) underline accent bar

### Tweet 7 Image — "The Full Simulation Pipeline"

(See base style above, then add:)

TITLE: "End-to-End Simulation Flow" — bold, black (#121212), sans-serif font, centered at the top.

LAYOUT: White background. A horizontal pipeline flowing left to right across the full width. 6 stages connected by bold black arrows. Each stage is a rectangle with a thick black border. The pipeline should be vertically centered with whitespace above and below.

STAGE 1:
- Small rectangle with a light yellow (#F0C020 at 15% opacity) fill
- Text inside: "Transaction" in bold black
- Small subtext: "User clicks Confirm" in gray

STAGE 2:
- Rectangle with a light blue (#1040C0 at 15% opacity) fill
- Text inside: "eth_createAccessList" in bold black
- Small subtext: "Discover touched contracts" in gray

STAGE 3:
- Rectangle with a white fill and thick black border
- Text inside: "Candidates" in bold black
- Small subtext: "[0xA, 0xB, 0xC...]" in monospace, gray

STAGE 4:
- Rectangle with a light blue (#1040C0 at 15% opacity) fill, slightly larger than the others — this is the key step
- Hard offset shadow (4px right, 4px down, solid black) to make it stand out
- Text inside: "eth_call + TxSimulator" in bold black
- Small subtext: "State override injection" in blue (#1040C0)

STAGE 5:
- Rectangle with a white fill and thick black border
- Text inside: "Deltas" in bold black
- Two lines of subtext:
  - "−500 USDC" in red (#D02020)
  - "+0.25 ETH" in blue (#1040C0)

STAGE 6:
- Rectangle with a light yellow (#F0C020 at 15% opacity) fill
- Text inside: "Enrich & Display" in bold black
- Small subtext: "Metadata + USD prices" in gray

BOTTOM LABEL:
- Centered below the pipeline: "Two RPC calls for simulation. Everything else is metadata." in bold black with a blue (#1040C0) underline accent bar

### Tweet 9 Image — "Storage Slot Discovery"

(See base style above, then add:)

TITLE: "Universal Storage Slot Discovery" — bold, black (#121212), sans-serif font, centered at the top.

LAYOUT: White background. A horizontal 3-stage flow, left to right, connected by bold black arrows. Vertically centered with whitespace.

STAGE 1 — LEFT:
- A rectangle with a thick black border and light yellow (#F0C020 at 15% opacity) fill
- Title inside: "eth_createAccessList" in bold black
- Below title, a code-style block with a light gray (#F0F0F0) background and thin black border:
  - Line 1: "to: USDC" in monospace
  - Line 2: "data: balanceOf(user)" in monospace
- This represents calling the access list on a balanceOf call

STAGE 2 — MIDDLE:
- A rectangle with a thick black border and white fill
- Title inside: "Returns Storage Keys" in bold black
- Below title, a code-style block with a light gray (#F0F0F0) background:
  - "storageKeys: [0x1a2b...]" in monospace
- A small annotation in blue (#1040C0): "This is the balance mapping slot"

STAGE 3 — RIGHT:
- A rectangle with a thick black border and light blue (#1040C0 at 15% opacity) fill
- Hard offset shadow (4px right, 4px down, solid black)
- Title inside: "Override & Retry" in bold black
- Two sub-items stacked vertically:
  - "Set slot to 10^30" with a yellow (#F0C020) square bullet
  - "Re-simulate" with a blue (#1040C0) checkmark icon and text "Success" in blue

BOTTOM LABEL:
- Centered below the pipeline: "Works for any ERC-20 — no hardcoded mappings needed" in bold black with a yellow (#F0C020) underline accent bar

### Tweet 12 Image

No generation needed — use a real screenshot of the WalletChan asset changes UI during a swap. Show the red "SEND" section with token amount and USD value, and the blue "RECEIVE" section. Authenticity over polish.
